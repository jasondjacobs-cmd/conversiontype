# Definition Bubbles testing

Static release checks run through npm test. Production browser QA should verify keyboard activation, Escape dismissal, focus restoration, click and tap behavior, viewport containment at supported mobile widths, unchanged canonical URLs, and no horizontal overflow.