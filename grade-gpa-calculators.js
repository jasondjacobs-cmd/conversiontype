(function(root){'use strict';const DEFAULT_SCALE={A:4,'A-':3.7,'B+':3.3,B:3,'B-':2.7,'C+':2.3,C:2,'C-':1.7,'D+':1.3,D:1,'D-':0.7,F:0};const round=x=>Math.round((x+Number.EPSILON)*100)/100;const gradePoints=(grade,scale=DEFAULT_SCALE)=>{const x=scale[grade];if(!Number.isFinite(x))throw Error('Choose a valid grade.');return x};const pointsGrade=rows=>{let earned=0,possible=0;for(const r of rows){if(r.earned<0||r.possible<=0)throw Error('Points earned must be zero or more and points possible must be greater than zero.');earned+=r.earned;possible+=r.possible}if(!rows.length)throw Error('Add at least one valid row.');return{grade:earned/possible*100,earned,possible}};const weightedGrade=rows=>{let contribution=0,weight=0;for(const r of rows){if(r.score<0||r.score>1000||r.weight<=0||r.weight>100)throw Error('Scores must be 0–1000% and each weight must be greater than 0 and no more than 100%.');contribution+=r.score*r.weight;weight+=r.weight}if(!rows.length)throw Error('Add at least one valid row.');if(weight>100+1e-9)throw Error('Weights cannot total more than 100%.');return{grade:contribution/weight,graded:weight,remaining:100-weight}};const requiredFinal=(current,target,weight)=>{if(weight<=0||weight>100)throw Error('Final exam weight must be greater than 0 and no more than 100%.');return(target-current*(1-weight/100))/(weight/100)};const projectedFinal=(current,finalScore,weight)=>{if(weight<=0||weight>100)throw Error('Final exam weight must be greater than 0 and no more than 100%.');return current*(1-weight/100)+finalScore*(weight/100)};const gpa=rows=>{let credits=0,quality=0;for(const r of rows){if(r.credits<=0)throw Error('Credits must be greater than zero.');quality+=r.points*r.credits;credits+=r.credits}if(!rows.length)throw Error('Add at least one valid course.');return{gpa:quality/credits,credits,quality}};const highSchool=rows=>{const base=gpa(rows.map(r=>({points:r.points,credits:r.credits}))),weighted=gpa(rows.map(r=>({points:r.points+r.boost,credits:r.credits})));return{unweighted:base.gpa,weighted:weighted.gpa,credits:base.credits}};const cumulative=(oldGpa,oldCredits,newRows)=>{if(oldCredits<0)throw Error('Completed credits cannot be negative.');if(oldCredits>0&&oldGpa<0)throw Error('Current GPA cannot be negative.');const fresh=gpa(newRows),credits=oldCredits+fresh.credits;return{gpa:(oldGpa*oldCredits+fresh.quality)/credits,credits,quality:oldGpa*oldCredits+fresh.quality}};const combineSemesters=rows=>gpa(rows.map(r=>({points:r.gpa,credits:r.credits})));root.GradeGpaEngine={DEFAULT_SCALE,gradePoints,pointsGrade,weightedGrade,requiredFinal,projectedFinal,gpa,highSchool,cumulative,combineSemesters};if(typeof document==='undefined')return;const form=document.querySelector('[data-grade-form]');if(!form)return;const kind=form.dataset.kind,result=document.querySelector('[data-grade-result]'),status=document.querySelector('[data-grade-status]'),share=document.querySelector('[data-share]');const num=(el,{zero=true,max=Infinity}={})=>{const x=Number(el?.value);if(!Number.isFinite(x)||(!zero&&x<=0)||x<0||x>max)throw Error('Enter valid values for all required fields.');return x};const fmt=x=>Number(x).toFixed(2);const cards=a=>`<div class="financial-results">${a.map(([k,v])=>`<div><span>${k}</span><strong>${v}</strong></div>`).join('')}</div>`;const rows=()=>[...form.querySelectorAll('[data-row]')];function scale(){const s={};form.querySelectorAll('[data-scale]').forEach(x=>{s[x.dataset.scale]=num(x)});return Object.keys(s).length?s:DEFAULT_SCALE}function courseRows(high=false){const s=scale();return rows().map(r=>{const grade=r.querySelector('[name="grade"]')?.value,credits=num(r.querySelector('[name="credits"]'),{zero:false});return{points:gradePoints(grade,s),credits,boost:high?num(r.querySelector('[name="level"]')):0}})}function calculate(update=true){try{let out=[];if(kind==='grade'){const mode=form.elements.mode.value;if(mode==='points'){const x=pointsGrade(rows().map(r=>({earned:num(r.querySelector('[name="earned"]')),possible:num(r.querySelector('[name="possible"]'),{zero:false})})));out=[['Overall grade',fmt(x.grade)+'%'],['Points earned',fmt(x.earned)],['Points possible',fmt(x.possible)]]}else{const x=weightedGrade(rows().map(r=>({score:num(r.querySelector('[name="score"]'),{max:1000}),weight:num(r.querySelector('[name="weight"]'),{zero:false,max:100})})));out=[['Current grade',fmt(x.grade)+'%'],['Weight graded',fmt(x.graded)+'%'],['Weight remaining',fmt(x.remaining)+'%']]}}if(kind==='final'){const mode=form.elements.mode.value,current=num(form.elements.current,{max:1000}),weight=num(form.elements.weight,{zero:false,max:100});if(mode==='required'){const x=requiredFinal(current,num(form.elements.target,{max:1000}),weight);let note=x>100?' Above 100% may require extra credit.':x<0?' Your target is already secured under these assumptions.':'';out=[['Required final score',fmt(x)+'%'+note]]}else out=[['Projected course grade',fmt(projectedFinal(current,num(form.elements.finalScore,{max:1000}),weight))+'%']]}if(kind==='gpa'){const x=gpa(courseRows());out=[['GPA',fmt(x.gpa)],['Total credits',fmt(x.credits)],['Quality points',fmt(x.quality)]]}if(kind==='highschool'){const x=highSchool(courseRows(true));out=[['Unweighted GPA',fmt(x.unweighted)],['Weighted GPA',fmt(x.weighted)],['Total credits',fmt(x.credits)]]}if(kind==='cumulative'){if(form.elements.mode.value==='existing'){const x=cumulative(num(form.elements.oldGpa),num(form.elements.oldCredits),courseRows());out=[['New cumulative GPA',fmt(x.gpa)],['Total credits',fmt(x.credits)],['Quality points',fmt(x.quality)]]}else{const x=combineSemesters(rows().map(r=>({gpa:num(r.querySelector('[name="semesterGpa"]')),credits:num(r.querySelector('[name="semesterCredits"]'),{zero:false})})));out=[['Cumulative GPA',fmt(x.gpa)],['Total credits',fmt(x.credits)]]}}result.innerHTML=cards(out);result.hidden=false;status.textContent='';if(update){const state={v:1,kind,values:{}};new FormData(form).forEach((v,k)=>{(state.values[k]??=[]).push(v)});const url=new URL(location.href);url.search='';url.searchParams.set('state',btoa(JSON.stringify(state)).replaceAll('+','-').replaceAll('/','_').replace(/=+$/,''));history.replaceState(null,'',url)}return true}catch(e){status.textContent=e.message;result.hidden=true;return false}}form.addEventListener('submit',e=>{e.preventDefault();calculate()});form.querySelector('[data-reset]')?.addEventListener('click',()=>{form.reset();result.hidden=true;status.textContent='';history.replaceState(null,'',location.pathname)});share?.addEventListener('click',async()=>{if(!result.hidden)try{await navigator.clipboard.writeText(location.href);share.textContent='Link copied'}catch{}});const raw=new URLSearchParams(location.search).get('state');if(raw)try{const p=raw.replaceAll('-','+').replaceAll('_','/')+'='.repeat((4-raw.length%4)%4),s=JSON.parse(atob(p));if(s?.v!==1||s.kind!==kind||!s.values||typeof s.values!=='object')throw Error();for(const[k,vals]of Object.entries(s.values)){const els=form.querySelectorAll(`[name="${CSS.escape(k)}"]`);if(!els.length||!Array.isArray(vals)||vals.length!==els.length)throw Error();els.forEach((el,i)=>el.value=vals[i])}if(!calculate(false))throw Error()}catch{status.textContent='This shared calculator link contains invalid values. You can still enter new values.'}})(typeof window!=='undefined'?window:globalThis);