import assert from 'node:assert/strict';await import('../construction-calculators.js');const E=globalThis.ConstructionEngine;assert.ok(E);assert.equal(E.volume(10,10,6,'ft','in'),50);assert.ok(Math.abs(E.area.circle(5)-Math.PI*25)<1e-12);assert.equal(E.boardFoot(2,6,8,10),80);const s=E.stairs(108,7.75,10);assert.equal(s.risers,14);assert.equal(s.treads,13);assert.equal(s.run,130);const p=E.roofPitch(6,12);assert.ok(Math.abs(p.angle-26.565051177)<1e-6);assert.ok(Math.abs(p.multiplier-Math.sqrt(1.25))<1e-12);const r=E.roofing(40,30,6,10);assert.equal(r.bundles,45);assert.equal(E.drywall(20,8,4,80,4,8,10).sheets,20);assert.equal(E.flooring(15,12,10,20).boxes,10);assert.equal(E.tile(10,8,12,12,0,10).tiles,88);assert.deepEqual(E.fence(100,8,2,5.5,.5),{sections:13,posts:14,rails:26,pickets:200});assert.equal(E.deck(16,12,5.5,.125,16,10).boards,29);assert.ok(Math.abs(E.asphalt(50,20,3,145,5).tons-19.03125)<1e-12);assert.equal(E.masonry(20,8,20,7.625,2.25,.375,5).count,1017);assert.equal(E.masonry(20,8,20,15.625,7.625,.375,5).count,166);console.log('Construction calculator formula tests: PASS');