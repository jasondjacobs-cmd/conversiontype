import assert from 'node:assert/strict';await import('../construction-calculators.js');const E=globalThis.ConstructionEngine;assert.ok(E);assert.equal(E.volume(10,10,6,'ft','in'),50);assert.ok(Math.abs(E.area.circle(5)-Math.PI*25)<1e-12);assert.equal(E.boardFoot(2,6,8,10),80);const s=E.stairs(108,7.75,10);assert.equal(s.risers,14);assert.equal(s.treads,13);assert.equal(s.run,130);const p=E.roofPitch(6,12);assert.ok(Math.abs(p.angle-26.565051177)<1e-6);assert.ok(Math.abs(p.multiplier-Math.sqrt(1.25))<1e-12);assert.equal(E.materialCount(180,20,10),10);assert.deepEqual(E.fence(100,8,2,5.5,.5),{sections:13,posts:14,rails:26,pickets:200});console.log('Construction calculator formula tests: PASS');