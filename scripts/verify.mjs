import{access,readFile}from'node:fs/promises';
const pages=['ratio-calculator','simplify-ratio','solve-ratio','scale-ratio','ratio-to-fraction','ratio-to-percentage','percentage-to-ratio'];
const required=['index.html','styles.css','app.js','ratio.js','robots.txt','sitemap.xml','site.webmanifest','favicon.ico','favicon-16x16.png','favicon-32x32.png','apple-touch-icon.png','android-chrome-192x192.png','android-chrome-512x512.png','assets/conversiontype-logo.png','.github/workflows/release-gates.yml',...pages.map(x=>`${x}/index.html`)];
await Promise.all(required.map(file=>access(file)));const html=await readFile('index.html','utf8'),js=await readFile('app.js','utf8'),ratio=await readFile('ratio.js','utf8'),map=await readFile('sitemap.xml','utf8'),manifest=JSON.parse(await readFile('site.webmanifest','utf8'));
const checks=[[html.includes('<meta name="description"'),'SEO description'],[html.includes('id="privacy"'),'privacy choices'],[html.includes('Ratio Calculator'),'ratio launch tool'],[js.includes('conversiontype.consent.v1'),'versioned consent'],[ratio.includes('data-ratio-form'),'ratio engine'],[!/(googletagmanager|doubleclick|adsbygoogle|google-analytics)/i.test(html+js+ratio),'no provider scripts'],[manifest.name==='ConversionType','web manifest'],...pages.map(p=>[map.includes(`https://conversiontype.com/${p}/`),`sitemap ${p}`])];
for(const p of pages){const body=await readFile(`${p}/index.html`,'utf8');checks.push([body.includes(`<link rel="canonical" href="https://conversiontype.com/${p}/">`),`${p} canonical`],[body.includes('<meta name="description"'),`${p} description`],[body.includes('<h1>'),`${p} h1`],[body.includes('application/ld+json'),`${p} structured data`],[body.includes('data-ratio-form'),`${p} calculator`])}
for(const[passed,name]of checks)if(!passed)throw new Error(`Release gate failed: ${name}`);console.log(`Passed ${checks.length} platform and SEO release checks.`);
