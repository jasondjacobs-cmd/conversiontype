import{readFile,access}from'node:fs/promises';import assert from'node:assert/strict';const pages=['construction-calculators','concrete-calculator','concrete-slab-calculator','square-footage-calculator','cubic-yard-calculator','gravel-calculator','mulch-calculator','paint-calculator','board-foot-calculator','stair-calculator'];for(const p of pages){await access(`${p}/index.html`);const h=await readFile(`${p}/index.html`,'utf8');assert.match(h,new RegExp(`<link rel="canonical" href="https://conversiontype.com/${p}/">`));assert.match(h,/<title>[^<]+<\/title>/);assert.match(h,/<h1>[^<]+<\/h1>/);if(p!=='construction-calculators'){assert.match(h,/data-construction-form/);assert.match(h,/>Calculate<\/button>/);assert.match(h,/data-share/);assert.match(h,/construction-calculators\.js/);assert.match(h,/(Formula|Method)/)}}const forbidden=['concrete-floor-calculator','concrete-driveway-calculator','driveway-gravel-calculator','square-feet-calculator','board-feet-calculator','stair-stringer-calculator'];for(const p of forbidden)await access(p).then(()=>assert.fail(`Forbidden page exists: ${p}`),()=>{});console.log('Construction page contract tests: PASS');